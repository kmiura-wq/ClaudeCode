# stagesync ルーティンのスケジュール設定

`talentio_client.py stagesync` を定期実行するルーティン（claude.ai/code/routines）の
スケジュール設定メモ。**時刻ズレの再発防止用**。

## 希望スケジュール

平日（月〜金）JST の 9時 / 11時 / 13時 / 15時 / 18時 / 19時 の 6 回。

## 重要：クラウド実行環境は UTC

ルーティンが動くコンテナの TZ は `Etc/UTC`（`TZ` 環境変数は未設定）。
cron 式を直接指定する場合（`/schedule update`）、**時刻は UTC で書く必要がある**。

| 希望 (JST) | cron に書く値 (UTC) |
| ---------- | ------------------- |
| 09:00      | 00:00               |
| 11:00      | 02:00               |
| 13:00      | 04:00               |
| 15:00      | 06:00               |
| 18:00      | 09:00               |
| 19:00      | 10:00               |

```
0 0,2,4,6,9,10 * * 1-5
```

JST 9〜19時は UTC 0〜10時（同じ暦日）に収まるため、曜日は UTC でもそのまま `1-5`
（月〜金）でよい。日付ズレは起きない。

### やってはいけない設定

`0 9,11,13,15,18,19 * * 1-5` と書くと JST 値がそのまま UTC として解釈され、
全便が +9 時間ズレる：

| cron (UTC) | 実際の JST      |
| ---------- | --------------- |
| 09:00      | 18:00 同日      |
| 11:00      | 20:00 同日      |
| 13:00      | 22:00 同日      |
| 15:00      | **翌 00:00**    |
| 18:00      | **翌 03:00**    |
| 19:00      | **翌 04:00**    |

金曜の遅い便が土曜 JST に落ちる（営業日外に実行）一方、月曜朝 JST には 1 便も
走らない、という副作用も出る。

> Web UI のプリセット（時刻ピッカー）で設定する場合はローカルタイムゾーンから
> 自動変換されるため UTC 換算は不要。ただしブラウザ／アカウントのタイムゾーンが
> Asia/Tokyo になっていることを必ず確認する。保存後に表示される「次回実行」が
> JST の想定時刻になっているかで検算できる。
>
> なお実行は stagger により数分遅れることがある（オフセットはルーティンごとに一定）。

## `--hours` の窓と実行間隔の関係

`--hours N` は「登録が N 時間以内の候補者」を対象にする。取りこぼしを防ぐには
**N ≧ 実行間隔の最大値** が必要。

修正後スケジュールでの間隔：

| 区間                  | 間隔     | `--hours 18` |
| --------------------- | -------- | ------------ |
| 9→11, 11→13, 13→15    | 2h       | OK           |
| 15→18                 | 3h       | OK           |
| 18→19                 | 1h       | OK           |
| 19→翌 9（平日夜間）   | 14h      | OK           |
| **金 19→月 9（週末）**| **62h**  | **NG**       |

### 週末の取りこぼし

金曜 19:00 の便は金 01:00 まで遡る。次は月曜 09:00 の便で、`--hours 18` だと
日曜 15:00 までしか遡らない。結果、**金 19:00 〜 日 15:00（44 時間）に登録された
候補者は恒久的に未処理**になる。

対策は `--hours` を週末が入る長さにすること（例 `--hours 72`）。
`stagesync` は既にステージがある候補者を `(stage already set)` でスキップするため、
窓を広げて同じ候補者を再スキャンしても二重登録にはならない（冪等）。
コストは候補者一覧のページング量が増える分のみ。

## 変更手順

ルーティンの設定はクラウド側（アカウント）に保存されており、実行中のセッション
内からは変更できない（Web セッション内では `/schedule` は使えない）。以下のどちらかで。

1. **Web UI**（推奨）: claude.ai/code/routines → 対象ルーティン → 鉛筆アイコン →
   **Select a trigger** で時刻を修正 → 保存 → 「次回実行」が JST 想定時刻か確認。
2. **ローカル端末の CLI**: `/schedule update` で上記 cron 式（UTC）を指定。
   ※ Web セッション内ではなくローカルのターミナルから実行する。
